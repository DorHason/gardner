<% include ../partials/header %>

<div class="container">
	<div class="row">
		<h1 style="text-align: center;">Edit project to <%= user.firstName %> <%= user.lastName %></h1>
		<div style="width: 30%; margin: 25px auto;">
			<form action="/users/<%= user.username %>/projects/<%= project.id %>?_method=PUT" method="POST">
				<!--The form-group groups things together and also gives us a little bit of space between them -->
				<div>
					Project title already exists
				</div>
				<div class="form-group">
					<input class="form-control" type="text" name="title" value="<%= project.id %>">
				</div>
				<!-- <div class="form-group">
					<input class="form-control" type="text" name="" value="<%= project.content %>" required>
				</div> -->
				<div class="form-group">
					<button class="btn btn-lg btn-default btn-block btn-primary">Save</button>
				</div>
			</form>
			<a href="/users/<%= user.username %>">Back</a>
		</div>
	</div>
</div>

<% include ../partials/footer %>